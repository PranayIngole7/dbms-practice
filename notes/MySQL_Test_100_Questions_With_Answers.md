# THE KIRAN ACADEMY, PUNE
### MYSQL TEST – 100 QUESTIONS
- Topics: DBMS, RDBMS, SQL, MySQL, DDL, DML, Constraints, Operators

> TOTAL: 100 QUESTIONS ; MCQ: 90 ; THEORY: 10
---
## SECTION A – DBMS (Q1–Q10)

Q1. What does DBMS stand for?
A. Database Management System ✅
B. Data Backup Management System
C. Database Monitoring System
D. Data Management Software

Q2. What is the primary purpose of a DBMS?
A. To create websites
B. To manage and organize data ✅
C. To design images 
D. To compile programs

Q3. Which of the following is an example of a DBMS?
A. MySQL ✅
B. HTML
C. CSS
D. JavaScript

Q4. Which component is responsible for storing and managing data in a DBMS?
A. Database Storage Engine ✅
B. Browser
C. Compiler
D. Operating System only

Q5. Which of the following is a benefit of a DBMS?
A. Data redundancy can be reduced ✅
B. Data cannot be shared
C. Data must always be duplicated
D. Security cannot be implemented

Q6. What is data redundancy?
A. Data encryption
B. Unnecessary duplication of data ✅
C. Data deletion
D. Data sorting

Q7. Which language is commonly used to communicate with relational databases?
A. SQL ✅
B. HTML
C. CSS
D. XML

Q8. Which database model stores data in tables?
A. Relational model ✅
B. Hierarchical model only
C. Network model only
D. File model

Q9. What is a database?
A. A collection of organized data ✅
B. A programming language
C. An operating system
D. A web browser

Q10. Which feature helps protect unauthorized access to database data?
A. Database security ✅
B. Data duplication
C. Data formatting
D. Data sorting
---

## SECTION B – RDBMS (Q11–Q20)

Q11. What does RDBMS stand for?
A. Relational Database Management System ✅
B. Remote Database Management System
C. Relational Data Backup Management System
D. Rapid Database Management System

Q12. In an RDBMS, data is primarily stored in:
A. Tables ✅
B. Images
C. Files only
D. Web pages

Q13. A row in a relational table is also called a:
A. Attribute
B. Record ✅
C. Column
D. Field type

Q14. A column in a relational table is also called a:
A. Record
B. Tuple
C. Attribute ✅
D. Database

Q15. Which key uniquely identifies each record in a table?
A. Foreign Key
B. Primary Key ✅
C. Candidate Key only
D. Alternate Key only

Q16. Which key is used to establish a relationship between two tables?
A. Primary Key
B. Foreign Key ✅
C. Super Key
D. Unique Key

Q17. Which constraint prevents duplicate values in a column?
A. UNIQUE ✅
B. DEFAULT
C. CHECK
D. NOT NULL

Q18. Can a table have more than one primary key?
A. Yes, unlimited
B. No, only one primary key constraint ✅
C. Yes, exactly two
D. Only three

Q19. A primary key generally cannot contain:
A. Integer values
B. Duplicate values ✅
C. String values
D. Unique values

Q20. Which relationship means one record in Table A can relate to many records in Table B?
A. One-to-one
B. One-to-many ✅
C. Many-to-one only
D. None
---

## SECTION C – SQL (Q21–Q30)

Q21. What does SQL stand for?
A. Structured Query Language ✅
B. Simple Query Language
C. Standard Question Language
D. System Query Language

Q22. Which command is used to retrieve data?
A. SELECT ✅
B. GET
C. FETCHDATA
D. SHOWDATA

Q23. Which command is used to add a new record?
A. ADD
B. INSERT ✅
C. CREATE
D. UPDATE

Q24. Which command modifies existing records?
A. CHANGE
B. MODIFY
C. UPDATE ✅
D. ALTER 

Q25. Which command removes records from a table?
A. REMOVE
B. DELETE ✅
C. DROP
D. CLEAR

Q26. Which SQL clause is used to filter records?
A. WHERE ✅
B. FILTER
C. HAVING ONLY
D. CONDITION

Q27. Which clause is used to sort query results?
A. SORT BY
B. ORDER BY ✅
C. GROUP BY 
D. ARRANGE BY

Q28. Which clause groups rows having the same values?
A. ORDER BY
B. GROUP BY ✅
C. WHERE
D. SORT BY

Q29. Which clause filters grouped results?
A. WHERE
B. HAVING ✅
C. FILTER
D. GROUP

Q30. Which symbol is commonly used to select all columns?
A. #
B. @
C. * ✅
D. %
---

## SECTION D – MySQL (Q31–Q40)

Q31. MySQL is primarily a:
A. Relational Database Management System ✅
B. Programming language
C. Web browser
D. Operating system

Q32. Which company developed MySQL?
A. Microsoft
B. Oracle ✅
C. Google
D. IBM

Q33. Which command displays available databases in MySQL?
A. SHOW DATABASES; ✅
B. DISPLAY DATABASES;
C. LIST DATABASE;
D. VIEW DATABASES;

Q34. Which command selects a database for use?
A. SELECT DATABASE
B. USE database_name; ✅
C. OPEN DATABASE
D. START DATABASE

Q35. Which command displays tables in the currently selected database?
A. SHOW TABLES; ✅
B. DISPLAY TABLES;
C. LIST TABLES;
D. GET TABLES;

Q36. Which command displays the structure of a table?
A. STRUCTURE table_name;
B. DESCRIBE table_name; ✅
C. TABLEINFO table_name;
D. SHOW STRUCTURE table_name;

Q37. Which port is commonly used by MySQL?
A. 80
B. 21
C. 3306 ✅
D. 8080

Q38. Which data type is commonly used for variable-length strings?
A. INT
B. VARCHAR ✅
C. DATE
D. FLOAT

Q39. Which data type is commonly used to store whole numbers?
A. INT ✅
B. VARCHAR
C. DATE
D. TEXT

Q40. Which data type stores date values?
A. VARCHAR
B. INT
C. DATE ✅
D. CHAR
---

## SECTION E – DDL COMMANDS (Q41–Q50)

Q41. What does DDL stand for?
A. Data Definition Language ✅
B. Data Development Language
C. Database Definition Logic
D. Data Description Language

Q42. Which command creates a table?
A. MAKE TABLE
B. CREATE TABLE ✅
C. NEW TABLE
D. ADD TABLE

Q43. Which command modifies the structure of an existing table?
A. UPDATE
B. ALTER ✅
C. MODIFY DATA
D. CHANGE TABLE DATA

Q44. Which command permanently removes a table structure?
A. DELETE
B. REMOVE
C. DROP ✅
D. CLEAR

Q45. Which command removes all rows while keeping the table structure?
A. DROP
B. TRUNCATE ✅
C. DELETE DATABASE
D. REMOVE TABLE

Q46. Which command creates a new database?
A. CREATE DATABASE ✅
B. NEW DATABASE
C. MAKE DATABASE
D. ADD DATABASE

Q47. Which command can add a new column to an existing table?
A. UPDATE TABLE
B. ALTER TABLE  ✅
C. INSERT COLUMN
D. CREATE COLUMN

Q48. Which command can rename a table in MySQL?
A. ALTER TABLE ... RENAME TO ✅
B. CHANGE TABLE
C. MODIFY TABLE NAME
D. UPDATE TABLE NAME

Q49. Which of the following belongs to DDL?
A. INSERT
B. UPDATE
C. CREATE ✅
D. DELETE

Q50. Which command removes the definition and data of a table?
A. DELETE
B. DROP ✅
C. UPDATE
D. SELECT
---

## SECTION F – DML COMMANDS (Q51–Q60)

Q51. What does DML stand for?
A. Data Manipulation Language ✅
B. Data Management Logic
C. Database Manipulation Language
D. Data Modification Logic

Q52. Which command inserts records into a table?
A. ADD
B. INSERT ✅
C. CREATE
D. ALTER

Q53. Which command changes existing data?
A. UPDATE ✅
B. ALTER
C. MODIFY TABLE
D. CHANGE DATABASE

Q54. Which command deletes selected records?
A. DROP
B. DELETE ✅
C. TRUNCATE TABLE ONLY
D. REMOVE DATABASE

Q55. Consider:
UPDATE student
SET city = 'Pune'
WHERE id = 101;
What does this query do?
A. Changes the city of student 101 to Pune ✅
B. Deletes student 101
C. Creates student 101
D. Changes the student ID

Q56. What happens if a DELETE statement is executed without a WHERE clause?
A. One record is deleted
B. All rows are deleted ✅
C. Table structure is deleted
D. Database is deleted

Q57. What happens if UPDATE is executed without a WHERE clause?
A. No record changes
B. Only the first record changes
C. All rows are updated with the specified values ✅
D. Table is dropped

Q58. Which statement adds a student?
A. INSERT INTO student VALUES (1,'Rahul'); ✅
B. ADD student VALUES (1,'Rahul');
C. CREATE student VALUES (1,'Rahul');
D. UPDATE student VALUES (1,'Rahul');

Q59. Which command is generally used to retrieve data?
A. INSERT
B. UPDATE
C. SELECT ✅
D. DELETE

Q60. Which command can remove selected rows based on a condition?
A. DELETE ✅
B. DROP
C. CREATE
D. ALTER
---

## SECTION G – CONSTRAINTS (Q61–Q75)

Q61. What is a constraint?
A. A rule applied to table data ✅
B. A programming language
C. A database server
D. A query editor

Q62. Which constraint uniquely identifies each row?
A. CHECK
B. DEFAULT
C. PRIMARY KEY ✅
D. NOT NULL

Q63. Which constraint prevents NULL values?
A. NOT NULL ✅
B. UNIQUE
C. DEFAULT 
D. CHECK

Q64. Which constraint prevents duplicate values?
A. DEFAULT
B. UNIQUE ✅
C. CHECK
D. NULL

Q65. Which constraint provides a value automatically when no value is supplied?
A. CHECK
B. DEFAULT ✅
C. UNIQUE
D. FOREIGN KEY

Q66. Which constraint validates a condition on data?
A. CHECK ✅
B. DEFAULT
C. PRIMARY
D. NULL

Q67. Which constraint maintains referential integrity?
A. UNIQUE
B. FOREIGN KEY ✅
C. CHECK
D. DEFAULT

Q68. CREATE TABLE student (
    id INT PRIMARY KEY,
    name VARCHAR(50)
);
What is the purpose of PRIMARY KEY?
A. Allows duplicate IDs
B. Uniquely identifies a student ✅
C. Stores only NULL values
D. Automatically deletes records

Q69. Which constraint can contain NULL values if no NOT NULL rule is specified?
A. PRIMARY KEY
B. Foreign key column
C. NOT NULL
D. None ✅

Q70. Can a table have multiple UNIQUE constraints?
A. Yes ✅
B. No
C. Only one
D. Only two

Q71. Which constraint is used to connect a child table with a parent table?
A. CHECK
B. FOREIGN KEY ✅
C. DEFAULT
D. UNIQUE

Q72. What does referential integrity ensure?
A. Valid relationships between related tables ✅
B. Duplicate records
C. Faster internet
D. Automatic backups only

Q73. Which definition correctly creates a NOT NULL column?
A. name VARCHAR(50) NOT NULL ✅
B. name NOT VARCHAR(50)
C. NOT NULL name VARCHAR(50)
D. name VARCHAR NOT

Q74. Which definition correctly creates a default value?
A. city VARCHAR(30) DEFAULT 'Pune' ✅
B. city DEFAULT VARCHAR(30)
C. DEFAULT city VARCHAR(30)
D. city VARCHAR(30) VALUE DEFAULT

Q75. Which constraint can be used to ensure age is greater than or equal to 18?
A. CHECK (age >= 18) ✅
B. DEFAULT (age >= 18)
C. UNIQUE (age >= 18)
D. PRIMARY (age >= 18)
---

## SECTION H – OPERATORS (Q76–Q90)


Q76. Which operator checks equality?
A. = ✅
B. != 
C. <>
D. Both A and C

Q77. Which operator means "not equal to" in MySQL?
A. !=
B. <>
C. Both A and B ✅
D. !== only

Q78. Which operator is used for greater than?
A. <
B. > ✅
C. >=
D. =

Q79. Which operator is used for less than or equal to?
A. <
B. <= ✅
C. >=
D. <>

Q80. Which operator is used to test whether a value falls within a range?
A. IN
B. BETWEEN ✅
C. RANGE
D. WITHIN

Q81. Which operator checks whether a value exists in a specified list?
A. IN ✅
B. BETWEEN
C. LIKE
D. EXISTS ONLY

Q82. Which operator is commonly used for pattern matching?
A. LIKE ✅
B. MATCHES
C. PATTERN
D. FIND

Q83. In LIKE, which wildcard represents any number of characters?
A. _ 
B. % ✅
C. *
D. #

Q84. In LIKE, which wildcard represents exactly one character?
A. %
B. _ ✅
C. *
D. ? 

Q85. Which logical operator requires both conditions to be true?
A. OR
B. NOT
C. AND ✅
D. XOR

Q86. Which logical operator requires at least one condition to be true?
A. AND
B. OR ✅
C. NOT
D. BETWEEN

Q87. Which operator reverses a Boolean condition?
A. AND
B. OR
C. NOT ✅
D. IN

Q88. What is the result of this condition?
WHERE salary > 30000 AND department = 'IT'
A. Salary must be greater than 30000 and department must be IT ✅
B. Either condition can be false
C. Only department is checked
D. Only salary is checked

Q89. Which query finds employees whose names start with A?
A. SELECT * FROM employee WHERE name LIKE 'A%'; ✅
B. SELECT * FROM employee WHERE name LIKE '%A';
C. SELECT * FROM employee WHERE name = 'A%';
D. SELECT * FROM employee WHERE name IN 'A%';

Q90. Which query finds employees with salary between 30000 and 50000?
A. WHERE salary IN 30000 TO 50000
B. WHERE salary BETWEEN 30000 AND 50000 ✅
C. WHERE salary RANGE 30000,50000
D. WHERE salary = 30000 AND 50000
---

## SECTION I – THEORY QUESTIONS (Q91–Q100)


Q91. Explain the difference between DBMS and RDBMS.

- Definition of DBMS : Software used to store, manage, and retrieve data. It stores data as flat files or in a hierarchical/network model.
- Definition of RDBMS : An advanced database system based on Edgar F. Codd's relational model. It stores data explicitly in related rows and columns.
- Data storage : DBMS stores data as flat files or trees with no enforced logical links between different data sheets. RDBMS organizes data strictly into structures called tables (relations).
- Tables and relationships : DBMS handles data tables independently. RDBMS links tables dynamically using logical relationships (One-to-One, One-to-Many, Many-to-Many).
- Keys : DBMS lacks strict relational key constraints. RDBMS relies heavily on keys like Primary Keys and Foreign Keys to enforce database integrity.
- Examples : DBMS includes XML, FoxPro, and MS Access. RDBMS includes MySQL, PostgreSQL, Oracle, and SQL Server.
- One real-world example : An Excel spreadsheet storing a single list of customer phone numbers behaves like a DBMS. An e-commerce system linking a Customers table to an Orders table via customer IDs behaves like an RDBMS.
---

Q92. What is a key in a database? Explain:
	Keys are attributes or sets of attributes used to identify rows uniquely and establish links between tables.
	
- Primary Key : Uniquely identifies each record in a table. It cannot contain NULL or duplicate values.
- Foreign Key : A column in a child table that references a Primary Key in a parent table to maintain referential integrity.
- Candidate Key : A column or minimal set of columns that qualifies to be a primary key because all its records are unique and non-null.
- Alternate Key : Candidate keys that were not chosen as the Primary Key.
- Composite Key : A Primary Key that consists of two or more columns combined together to ensure uniqueness.
- Unique Key : Enforces uniqueness on a column so no duplicate values are allowed. Unlike a primary key, it can accept one NULL value. 
---

Q93. What is SQL? Explain the major categories of SQL commands. Give at least two commands/examples for each category.
- DDL (Data Definition Language): Defines, modifies, and manages the physical structure of database objects like tables. 
> Examples: CREATE, ALTER, DROP
- DML (Data Manipulation Language): Modifies, adds, and removes the actual data stored inside the tables. 
> Examples: INSERT, UPDATE, DELETE
- DQL (Data Query Language): Used exclusively to fetch and retrieve data from the database. 
> Examples: SELECT
- DCL (Data Control Language): Manages user access, permissions, and security rights for the database system. 
> Examples: GRANT, REVOKE
- TCL (Transaction Control Language): Manages transactions and changes made by DML statements to guarantee database consistency.
> Examples: COMMIT, ROLLBACK
---

Q94. Explain DDL commands with suitable SQL examples:
- CREATE : Builds a brand new table or database object.
```sql
CREATE TABLE items (item_id INT PRIMARY KEY, name VARCHAR(50));
```
- ALTER : Modifies the structural blueprint of an existing table.
```sql
ALTER TABLE items ADD price DECIMAL(10,2);
```
- DROP : Permanently deletes a table structure along with all its data from the system.
```sql
DROP TABLE items;
```
- TRUNCATE : Removes all rows from a table immediately while preserving the original column structure.
```sql
TRUNCATE TABLE items;
```
---

Q95. Explain DML commands using a student table, Write one practical SQL query for each.
- INSERT : Adds new data rows into a table.
```sql
INSERT INTO student (id, name, city) VALUES (101, 'Aarav Kumar', 'Mumbai');
```
- UPDATE :  Modifies existing records based on a specific condition.
```sql 
UPDATE student SET city = 'Pune' WHERE id = 101;
```
- DELETE : Removes specific records from a table based on a condition.
```sql
DELETE FROM student WHERE id = 101;
```
---

Q96. What are constraints in MySQL? Explain:
- PRIMARY KEY
- FOREIGN KEY
- NOT NULL
- UNIQUE
- CHECK
- DEFAULT
Explain why constraints are important in a real-world database.

### 1. What are Constraints in MySQL?
**Constraints** are rules or conditions applied to table columns in MySQL to restrict the type of data that can be inserted into or updated in a table. They ensure data accuracy, consistency, integrity, and reliability across the database system.

---

### 2. Explanation of Key Constraints

#### **PRIMARY KEY**
* **Definition:** A field or combination of fields that uniquely identifies each record/row in a table.
* **Key Characteristics:**
  * Must contain **unique** values.
  * Cannot contain `NULL` values.
  * A table can have **only one** Primary Key.
* **Example:**
  ```sql
  CREATE TABLE department (
      dept_id INT PRIMARY KEY,
      dept_name VARCHAR(50)
  );
  ```

#### **FOREIGN KEY**
* **Definition:** A column or set of columns in one table that references the `PRIMARY KEY` of another table. It establishes a link/relationship between two tables.
* **Key Characteristics:**
  * Enforces **referential integrity** (prevents orphan records).
  * Prevents inserting values that do not exist in the referenced parent table.
* **Example:**
  ```sql
  CREATE TABLE employee (
      emp_id INT PRIMARY KEY,
      emp_name VARCHAR(50),
      dept_id INT,
      FOREIGN KEY (dept_id) REFERENCES department(dept_id)
  );
  ```

#### **NOT NULL**
* **Definition:** Ensures that a column cannot store `NULL` (empty/missing) values.
* **Key Characteristics:** A value must always be provided for this column during insertion.
* **Example:**
  ```sql
  CREATE TABLE users (
      user_id INT PRIMARY KEY,
      username VARCHAR(50) NOT NULL
  );
  ```

#### **UNIQUE**
* **Definition:** Ensures all values in a specific column are distinct from one another.
* **Key Characteristics:**
  * Unlike `PRIMARY KEY`, a table can have multiple `UNIQUE` constraints.
  * Depending on MySQL configuration, `UNIQUE` columns can accept `NULL` values.
* **Example:**
  ```sql
  CREATE TABLE users (
      user_id INT PRIMARY KEY,
      email VARCHAR(100) UNIQUE
  );
  ```

#### **CHECK**
* **Definition:** Ensures that all values in a column satisfy a specific logical condition.
* **Key Characteristics:** Prevents invalid data entries that violate the condition.
* **Example:**
  ```sql
  CREATE TABLE account (
      acc_id INT PRIMARY KEY,
      balance DECIMAL(10,2),
      CHECK (balance >= 0)
  );
  ```

#### **DEFAULT**
* **Definition:** Provides a default predefined value for a column if no explicit value is specified during an `INSERT` statement.
* **Example:**
  ```sql
  CREATE TABLE orders (
      order_id INT PRIMARY KEY,
      status VARCHAR(20) DEFAULT 'Pending',
      order_date DATE DEFAULT (CURRENT_DATE)
  );
  ```

---

### 3. Importance of Constraints in Real-World Databases
1. **Data Integrity:** Constraints enforce strict data rules at the database level rather than relying solely on application software logic.
2. **Prevention of Invalid Data:** Rules like `CHECK` and `NOT NULL` block corruption and incomplete data input at the source.
3. **Prevention of Duplication:** Constraints like `PRIMARY KEY` and `UNIQUE` eliminate redundant entries.
4. **Referential Consistency:** `FOREIGN KEY` constraints preserve relations across relational tables and prevent orphan records during deletions or updates.
5. **Business Logic Enforcement:** Constraints enforce business boundaries directly within the schema design (e.g., age restrictions, non-negative account balances).

---

Q97. Explain SQL operators with examples:
- Arithmetic operators
- Comparison operators
- Logical operators
- BETWEEN
- IN
- LIKE

### SQL Operators with Examples

#### 1. Arithmetic Operators
Used to perform mathematical operations on numerical data.

* `+` (Addition)
* `-` (Subtraction)
* `*` (Multiplication)
* `/` (Division)
* `%` or `MOD` (Modulus / Remainder)

**Examples:**
```sql
-- Calculate bonus as 10% of salary
SELECT emp_name, salary, (salary * 0.10) AS bonus FROM employee;

-- Calculate daily wage (assuming 30 working days)
SELECT emp_name, (salary / 30) AS daily_wage FROM employee;
```

---

#### 2. Comparison Operators
Used to compare two values, returning a Boolean truth value (`TRUE`, `FALSE`, or `NULL`).

* `=` (Equal to)
* `!=` or `<>` (Not equal to)
* `>` (Greater than)
* `<` (Less than)
* `>=` (Greater than or equal to)
* `<=` (Less than or equal to)

**Examples:**
```sql
-- Find employees earning exactly 60,000
SELECT * FROM employee WHERE salary = 60000;

-- Find employees with salary greater than or equal to 45,000
SELECT * FROM employee WHERE salary >= 45000;
```

---

#### 3. Logical Operators
Used to combine multiple condition predicates in a SQL clause.

* `AND` (Returns TRUE if all combined conditions are TRUE)
* `OR` (Returns TRUE if at least one condition is TRUE)
* `NOT` (Inverts the logical evaluation of a condition)

**Examples:**
```sql
-- AND: Find employees in IT department earning over 50,000
SELECT * FROM employee WHERE department = 'IT' AND salary > 50000;

-- OR: Find employees in IT or HR departments
SELECT * FROM employee WHERE department = 'IT' OR department = 'HR';

-- NOT: Find employees NOT in Sales department
SELECT * FROM employee WHERE NOT department = 'Sales';
```

---

#### 4. BETWEEN Operator
Filters range matches (inclusive of boundaries).

**Syntax:** `expression BETWEEN low_value AND high_value`

**Example:**
```sql
-- Find employees with salary between 30,000 and 60,000 (inclusive)
SELECT * FROM employee WHERE salary BETWEEN 30000 AND 60000;
```

---

#### 5. IN Operator
Checks whether a given column value matches any value in a defined list.

**Syntax:** `expression IN (value1, value2, ...)`

**Example:**
```sql
-- Find employees located in Pune, Mumbai, or Bangalore
SELECT * FROM employee WHERE city IN ('Pune', 'Mumbai', 'Bangalore');
```

---

#### 6. LIKE Operator
Used for pattern matching with text wildcard strings.

* `%` represents zero, one, or multiple characters.
* `_` represents a single character.

**Examples:**
```sql
-- Name starts with 'A'
SELECT * FROM employee WHERE emp_name LIKE 'A%';

-- Name ends with 'n'
SELECT * FROM employee WHERE emp_name LIKE '%n';

-- Second letter of name is 'o'
SELECT * FROM employee WHERE emp_name LIKE '_o%';
```

---

Q98. Consider:
employee(emp_id, emp_name, department, salary, city, experience)

Write SQL queries to:
1. Display all employees.
2. Display employees from Pune.
3. Display employees having salary greater than 50,000.
4. Display employees from IT department.
5. Display employees with experience between 2 and 5 years.
6. Display employees whose name starts with A.
7. Display employees from Pune OR Mumbai.

### SQL Queries

```sql
-- 1. Display all employees
SELECT * FROM employee;

-- 2. Display employees from Pune
SELECT * FROM employee 
WHERE city = 'Pune';

-- 3. Display employees having salary greater than 50,000
SELECT * FROM employee 
WHERE salary > 50000;

-- 4. Display employees from IT department
SELECT * FROM employee 
WHERE department = 'IT';

-- 5. Display employees with experience between 2 and 5 years
SELECT * FROM employee 
WHERE experience BETWEEN 2 AND 5;

-- 6. Display employees whose name starts with 'A'
SELECT * FROM employee 
WHERE emp_name LIKE 'A%';

-- 7. Display employees from Pune OR Mumbai
SELECT * FROM employee 
WHERE city = 'Pune' OR city = 'Mumbai';
-- Alternative syntax:
-- SELECT * FROM employee WHERE city IN ('Pune', 'Mumbai');
```

---

Q99. Design a Student Management Database.
Include:
1. Database name
2. At least 3 tables
3. At least 5 fields per table
4. Primary keys
5. Foreign keys
6. Suitable data types
7. UNIQUE constraint
8. NOT NULL constraint
9. DEFAULT constraint
10. CREATE DATABASE and CREATE TABLE statements

### Student Management Database Design

1. **Database Name:** `student_management_db`
2. **Tables:**
   * `department`
   * `student`
   * `course_enrollment`

```sql
-- Create the Database
CREATE DATABASE student_management_db;
USE student_management_db;

-- Table 1: Department Table
CREATE TABLE department (
    dept_id INT AUTO_INCREMENT PRIMARY KEY,            -- PRIMARY KEY
    dept_name VARCHAR(100) NOT NULL UNIQUE,            -- NOT NULL, UNIQUE
    dept_code VARCHAR(10) NOT NULL UNIQUE,             -- NOT NULL, UNIQUE
    building VARCHAR(50) DEFAULT 'Main Block',         -- DEFAULT
    head_of_dept VARCHAR(100) NOT NULL                 -- NOT NULL
);

-- Table 2: Student Table
CREATE TABLE student (
    student_id INT AUTO_INCREMENT PRIMARY KEY,         -- PRIMARY KEY
    first_name VARCHAR(50) NOT NULL,                   -- NOT NULL
    last_name VARCHAR(50) NOT NULL,                    -- NOT NULL
    email VARCHAR(100) NOT NULL UNIQUE,                -- NOT NULL, UNIQUE
    phone_number VARCHAR(15) UNIQUE,                   -- UNIQUE
    enrollment_date DATE DEFAULT (CURRENT_DATE),       -- DEFAULT
    dept_id INT NOT NULL,                              -- NOT NULL
    is_active BOOLEAN DEFAULT TRUE,                    -- DEFAULT
    FOREIGN KEY (dept_id) REFERENCES department(dept_id) -- FOREIGN KEY
);

-- Table 3: Course Enrollment Table
CREATE TABLE course_enrollment (
    enrollment_id INT AUTO_INCREMENT PRIMARY KEY,      -- PRIMARY KEY
    student_id INT NOT NULL,                           -- NOT NULL
    course_code VARCHAR(20) NOT NULL,                  -- NOT NULL
    semester INT NOT NULL,                             -- NOT NULL
    academic_year INT DEFAULT 2026,                    -- DEFAULT
    status VARCHAR(20) DEFAULT 'Enrolled',             -- DEFAULT
    FOREIGN KEY (student_id) REFERENCES student(student_id) -- FOREIGN KEY
);
```

---


Q100. A company wants an Employee Management Database with:
- Employee ID
- Employee name
- Email
- Mobile number
- Department
- Salary
- Joining date
- City

Write SQL to:
1. Create the database.
2. Create the employee table.
3. Apply appropriate constraints.
4. Insert at least 5 employees.
5. Update one employee's salary.
6. Delete one employee.
7. Find employees earning more than 50,000.
8. Find employees from Pune.
9. Find employees whose names start with S.
10. Find employees whose salary is between 30,000 and 60,000.

### Employee Management Database

```sql
-- 1. Create Database and Switch Context
CREATE DATABASE emp_mngmt_db;
USE emp_mngmt_db;

-- 2 & 3. Create Employee Table with Appropriate Constraints
CREATE TABLE employee (
    emp_id INT AUTO_INCREMENT PRIMARY KEY,              -- Primary Key
    emp_name VARCHAR(100) NOT NULL,                     -- NOT NULL
    email VARCHAR(100) NOT NULL UNIQUE,                 -- NOT NULL, UNIQUE
    mobile_no VARCHAR(15) NOT NULL UNIQUE,              -- NOT NULL, UNIQUE
    department VARCHAR(50) NOT NULL,                    -- NOT NULL
    salary DECIMAL(10,2) NOT NULL CHECK (salary >= 0),  -- NOT NULL, CHECK constraint
    joining_date DATE DEFAULT (CURRENT_DATE),          -- DEFAULT
    city VARCHAR(50) DEFAULT 'Pune'                     -- DEFAULT
);

-- 4. Insert at least 5 employees
INSERT INTO employee (emp_id, emp_name, email, mobile_no, department, salary, joining_date, city) VALUES
(101, 'Siddharth Sharma', 'siddharth@company.com', '9876543210', 'IT', 65000.00, '2023-01-15', 'Pune'),
(102, 'Priya Patel', 'priya@company.com', '9876543211', 'HR', 45000.00, '2023-03-20', 'Mumbai'),
(103, 'Suresh Kumar', 'suresh@company.com', '9876543212', 'IT', 55000.00, '2022-11-10', 'Pune'),
(104, 'Ananya Deshmukh', 'ananya@company.com', '9876543213', 'Finance', 70000.00, '2021-06-01', 'Delhi'),
(105, 'Sameer Joshi', 'sameer@company.com', '9876543214', 'Sales', 35000.00, '2024-02-12', 'Pune'),
(106, 'Rahul Verma', 'rahul@company.com', '9876543215', 'Marketing', 28000.00, '2024-05-18', 'Bangalore');

-- 5. Update one employee's salary
UPDATE employee 
SET salary = 58000.00 
WHERE emp_id = 103;

-- 6. Delete one employee
DELETE FROM employee 
WHERE emp_id = 106;

-- 7. Find employees earning more than 50,000
SELECT * FROM employee 
WHERE salary > 50000;

-- 8. Find employees from Pune
SELECT * FROM employee 
WHERE city = 'Pune';

-- 9. Find employees whose names start with 'S'
SELECT * FROM employee 
WHERE emp_name LIKE 'S%';

-- 10. Find employees whose salary is between 30,000 and 60,000
SELECT * FROM employee 
WHERE salary BETWEEN 30000 AND 60000;
```
---
